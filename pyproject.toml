[project]
name = "cppp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=7.2.0",
    "lqg",
    "seaborn>=0.13.2",
]

[tool.uv.sources]
lqg = { git = "https://github.com/RothkopfLab/lqg.git" }

[tool.uv.build-backend]
module-root = ""

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"